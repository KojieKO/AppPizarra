# 1.0.03 · Minitarea 1/5 · Identidad y Acerca de

Fecha: 23/09/2026. Alcance terminado: identidad y Acerca de. Base: fuentes actuales 1.0.02 y estados SUKI-01 a SUKI-05. No se han iniciado las minitareas siguientes.

## Cambios

- Nombre visible `Pizarra Pro` en título y cabecera, sin versión ni etiqueta PORTABLE en la cabecera.
- Versión `1.0.03` declarada una sola vez en el proyecto; `AppVersion.Display` sigue leyendo AssemblyInformationalVersion de la compilación. `AppVersion.Name`, `WindowTitle` y `Product` centralizan su uso en interfaz, cliente Drive y productor de PDF.
- Metadatos Product y Title del ensamblado: `Pizarra Pro`. Se conservan AssemblyName, namespaces, manifiesto, identificadores, rutas y configuración existentes.
- Acceso `Opciones → Acerca de Pizarra Pro`, con nombre y versión real. Reutiliza SecondaryUi, diálogo modal, botón Aceptar y cierre con Escape. Factoría `MainWindow.CreateAboutWindow` y acción `ShowAbout` reutilizables para la futura página Opciones.
- Archivos modificados: AppVersion.cs, PizarrasPro.csproj, MainWindow.cs, CloudDrive.cs y tests/PizarrasPro.ThemeSmoke/Program.cs. No se modificaron archivos personales ni la hoja de ruta.

## Comprobaciones

- Compilación y prueba nativa de interfaz correctas mediante el proyecto ThemeSmoke, tamaño mínimo 900×560. Se reutilizó el entorno .tools y las dependencias existentes.
- Verificados nombre de ventana, nombre y ausencia de versión en cabecera, versión informativa 1.0.03, metadatos Product y cadena del productor PDF, presencia de la entrada Acerca de en Opciones, contenido del diálogo abierto y cierre mediante Aceptar.
- Comprobaciones existentes de disposición, seis columnas, controles y recursos SukiUI correctas; sin solapamiento del pie y cabecera dentro de sus límites.
- Capturas revisadas visualmente: `validation/1.0.03-01-main.png` y `validation/1.0.03-01-main.about.png`. Nombre legible y versión correcta en Acerca de.
- Comando: `.tools/dotnet/dotnet.exe run --no-restore --project tests/PizarrasPro.ThemeSmoke -p:UsedAvaloniaProducts= -- docs/validation/1.0.03-01-main.png minimum`, con DOTNET_CLI_HOME=.tools/home y NUGET_PACKAGES=.tools/packages. Salida final 0.

## Límites

- Prueba con contenido real de MainWindow alojado en ventana de ensayo y diálogo real creado por su factoría; no ejecuta escaneo ni escritura de preferencias. El acceso de menú se comprobó estructuralmente, no mediante clic de usuario de extremo a extremo. No se repitió toda la batería funcional ni se probó otro dispositivo.
- Aviso NU1900: no se pudo consultar el índice de NuGet para auditoría de vulnerabilidades. La compilación y prueba terminaron correctamente; no se desactivó la auditoría.
- No se publicó ejecutable portable ni ZIP. El script de publicación conserva sus valores 1.0.02; la minitarea 5 deberá actualizar nombre, versión y destino antes de ejecutarlo, preferiblemente leyendo la versión del proyecto para evitar duplicarla. No ejecutar sus valores predeterminados para entregar 1.0.03.
- Directorio sin repositorio Git; no hay diff de Git disponible.

## Traspaso

Continuar únicamente cuando el usuario inicie la minitarea 2: temas Luminoso, Oscuro y Según el sistema. Mantener esta identidad y el acceso Acerca de; las minitareas 3 y 4 podrán trasladarlo a Opciones. La prueba ThemeSmoke sigue comprobando Light/Blue al inicio y deberá adaptarse al introducir preferencias de tema. La minitarea 5 realizará validación completa y entrega portable. La hoja de ruta 1.1.00 permanece intacta.
